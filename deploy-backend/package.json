{
  "name": "cantale-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend API pour le site Cantale — auth par code in-game, profils, leaderboards, factions, boutique",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "mysql2": "^3.11.4"
  }
}