import { RegistreLoading } from "@/components/ui/registre-loading";

export default function Loading() {
  return <RegistreLoading kicker="Registre — Recrutement" label="Équipe en cours de lecture…" />;
}
